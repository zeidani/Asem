
/* =====================================
   GLOBAL LANGUAGE SYSTEM
   EN / AR / IT / ZH / HI
   Header order remains LTR in every language.
===================================== */
const SITE_TRANSLATIONS = {
  "Home":{ar:"الرئيسية",it:"Home",zh:"首页",hi:"होम"},
  "About":{ar:"نبذة",it:"Profilo",zh:"简介",hi:"परिचय"},
  "Resume":{ar:"السيرة الذاتية",it:"Curriculum",zh:"简历",hi:"रिज़्यूमे"},
  "Portfolio":{ar:"الأنشطة السريرية",it:"Attività cliniche",zh:"临床活动",hi:"क्लिनिकल गतिविधियाँ"},
  "Services":{ar:"الخدمات الطبية",it:"Servizi medici",zh:"医疗服务",hi:"चिकित्सा सेवाएँ"},
  "Virtual Assistant":{ar:"المساعد الافتراضي",it:"Assistente virtuale",zh:"虚拟助手",hi:"वर्चुअल सहायक"},
  "Contact":{ar:"التواصل",it:"Contatti",zh:"联系",hi:"संपर्क"},
  "Coming Soon":{ar:"قريبًا",it:"Prossimamente",zh:"即将推出",hi:"जल्द आ रहा है"},
  "The virtual medical assistant is currently being prepared.":{ar:"يتم حاليًا إعداد المساعد الطبي الافتراضي.",it:"L'assistente medico virtuale è attualmente in preparazione.",zh:"虚拟医疗助手正在准备中。",hi:"वर्चुअल चिकित्सा सहायक अभी तैयार किया जा रहा है।"},
  "General Practitioner":{ar:"طبيب عام",it:"Medico di medicina generale",zh:"全科医生",hi:"सामान्य चिकित्सक"},
  "DR. ASEM":{ar:"د. عاصم",it:"DOTT. ASEM",zh:"阿西姆医生",hi:"डॉ. आसिम"},
  "Primary Care | Urgent Care":{ar:"الرعاية الأولية | الرعاية العاجلة",it:"Cure primarie | Cure urgenti",zh:"初级保健 | 紧急护理",hi:"प्राथमिक देखभाल | तत्काल देखभाल"},
  "About Me":{ar:"نبذة عني",it:"Profilo professionale",zh:"个人简介",hi:"मेरे बारे में"},
  "Professional Resume":{ar:"السيرة الذاتية المهنية",it:"Curriculum professionale",zh:"专业简历",hi:"पेशेवर रिज़्यूमे"},
  "Professional Experience":{ar:"الخبرة المهنية",it:"Esperienza professionale",zh:"专业经历",hi:"पेशेवर अनुभव"},
  "Clinical Expertise":{ar:"الخبرات السريرية",it:"Competenze cliniche",zh:"临床专长",hi:"क्लिनिकल विशेषज्ञता"},
  "Patient Assessment":{ar:"تقييم المرضى",it:"Valutazione del paziente",zh:"患者评估",hi:"रोगी मूल्यांकन"},
  "Primary & Urgent Care":{ar:"الرعاية الأولية والعاجلة",it:"Cure primarie e urgenti",zh:"初级与紧急护理",hi:"प्राथमिक और तत्काल देखभाल"},
  "Chronic Disease Management":{ar:"إدارة الأمراض المزمنة",it:"Gestione delle malattie croniche",zh:"慢性病管理",hi:"दीर्घकालिक रोग प्रबंधन"},
  "Preventive Care":{ar:"الرعاية الوقائية",it:"Cure preventive",zh:"预防保健",hi:"निवारक देखभाल"},
  "Professional Development":{ar:"التطوير المهني",it:"Sviluppo professionale",zh:"专业发展",hi:"व्यावसायिक विकास"},
  "View Projects":{ar:"عرض الأنشطة",it:"Vedi attività",zh:"查看活动",hi:"गतिविधियाँ देखें"},
  "Let's Talk":{ar:"تواصل معي",it:"Contattami",zh:"联系我",hi:"संपर्क करें"},
  "Download CV":{ar:"تحميل السيرة الذاتية",it:"Scarica il CV",zh:"下载简历",hi:"सीवी डाउनलोड करें"},
  "Contact Me":{ar:"تواصل معي",it:"Contattami",zh:"联系我",hi:"मुझसे संपर्क करें"},
  "Email":{ar:"البريد الإلكتروني",it:"Email",zh:"电子邮件",hi:"ईमेल"},
  "Phone":{ar:"الهاتف",it:"Telefono",zh:"电话",hi:"फ़ोन"},
  "Location":{ar:"الموقع",it:"Sede",zh:"地点",hi:"स्थान"},
  "Manama, Bahrain":{ar:"المنامة، البحرين",it:"Manama, Bahrein",zh:"巴林麦纳麦",hi:"मनामा, बहरीन"},
  "Providing patient-centered primary and urgent care through careful assessment, evidence-based treatment and clear communication.":{ar:"تقديم رعاية أولية وعاجلة تتمحور حول المريض من خلال التقييم الدقيق والعلاج المبني على الدليل والتواصل الواضح.",it:"Assistenza primaria e urgente centrata sul paziente, con valutazione accurata, terapie basate sull'evidenza e comunicazione chiara.",zh:"通过细致评估、循证治疗和清晰沟通，提供以患者为中心的初级与紧急护理。",hi:"सावधानीपूर्वक मूल्यांकन, प्रमाण-आधारित उपचार और स्पष्ट संवाद के माध्यम से रोगी-केंद्रित प्राथमिक एवं तत्काल देखभाल।"},
  "I am Dr. Asem Al-Zaidani, a General Practitioner focused on patient assessment, primary and urgent care, chronic-disease follow-up, preventive medicine and continuous professional development.":{ar:"أنا د. عاصم الزيداني، طبيب عام أركز على تقييم المرضى والرعاية الأولية والعاجلة ومتابعة الأمراض المزمنة والطب الوقائي والتطوير المهني المستمر.",it:"Sono il Dott. Asem Al-Zaidani, medico di medicina generale, con attività nella valutazione del paziente, nelle cure primarie e urgenti, nel follow-up delle patologie croniche e nella prevenzione.",zh:"我是阿西姆·扎伊达尼医生，一名全科医生，专注于患者评估、初级与紧急护理、慢性病随访、预防医学及持续专业发展。",hi:"मैं डॉ. आसिम अल-ज़ैदानी हूँ, एक सामान्य चिकित्सक, और रोगी मूल्यांकन, प्राथमिक व तत्काल देखभाल, दीर्घकालिक रोग फॉलो-अप, निवारक चिकित्सा तथा सतत व्यावसायिक विकास पर केंद्रित हूँ।"},
  "I currently practice at Dar Al Shifa Medical Centre in Manama, Bahrain, delivering compassionate, evidence-based care for patients of all ages.":{ar:"أعمل حاليًا في مركز دار الشفاء الطبي بالمنامة، البحرين، وأقدم رعاية إنسانية مبنية على الدليل للمرضى من جميع الأعمار.",it:"Attualmente esercito presso il Dar Al Shifa Medical Centre di Manama, Bahrein, offrendo cure attente e basate sull'evidenza a pazienti di tutte le età.",zh:"我目前在巴林麦纳麦的达尔希法医疗中心执业，为各年龄段患者提供富有同理心的循证医疗服务。",hi:"मैं वर्तमान में मनामा, बहरीन के दार अल शिफा मेडिकल सेंटर में कार्यरत हूँ और सभी आयु वर्ग के रोगियों को संवेदनशील, प्रमाण-आधारित देखभाल प्रदान करता हूँ।"},
  "I value clear communication, careful clinical reasoning and collaborative care that respects each patient's needs and circumstances.":{ar:"أؤمن بالتواصل الواضح والاستدلال السريري الدقيق والرعاية التعاونية التي تحترم احتياجات كل مريض وظروفه.",it:"Attribuisco grande valore alla comunicazione chiara, al ragionamento clinico accurato e a cure condivise che rispettino le esigenze di ogni paziente.",zh:"我重视清晰沟通、严谨的临床推理，以及尊重每位患者需求与实际情况的协作式护理。",hi:"मैं स्पष्ट संवाद, सावधानीपूर्ण क्लिनिकल निर्णय और प्रत्येक रोगी की आवश्यकताओं व परिस्थितियों का सम्मान करने वाली सहयोगी देखभाल को महत्व देता हूँ।"},
  "General Practitioner experienced in patient assessment, primary and urgent care, preventive medicine, and chronic-disease follow-up.":{ar:"طبيب عام ذو خبرة في تقييم المرضى والرعاية الأولية والعاجلة والطب الوقائي ومتابعة الأمراض المزمنة.",it:"Medico di medicina generale con esperienza nella valutazione del paziente, nelle cure primarie e urgenti, nella prevenzione e nel follow-up delle patologie croniche.",zh:"在患者评估、初级与紧急护理、预防医学及慢性病随访方面经验丰富的全科医生。",hi:"रोगी मूल्यांकन, प्राथमिक व तत्काल देखभाल, निवारक चिकित्सा और दीर्घकालिक रोग फॉलो-अप में अनुभवी सामान्य चिकित्सक।"},
  "Committed to evidence-based treatment, clear patient communication and coordinated follow-up that supports positive health outcomes.":{ar:"ملتزم بالعلاج المبني على الدليل والتواصل الواضح مع المرضى والمتابعة المنسقة لدعم نتائج صحية إيجابية.",it:"Impegnato in trattamenti basati sull'evidenza, comunicazione chiara e follow-up coordinato per favorire risultati di salute positivi.",zh:"致力于循证治疗、清晰的医患沟通和协调随访，以促进良好的健康结果。",hi:"सकारात्मक स्वास्थ्य परिणामों के लिए प्रमाण-आधारित उपचार, स्पष्ट रोगी संवाद और समन्वित फॉलो-अप के प्रति प्रतिबद्ध।"},
  "Let's discuss your healthcare needs and the appropriate next step.":{ar:"دعنا نناقش احتياجاتك الصحية والخطوة المناسبة التالية.",it:"Parliamo delle tue esigenze di salute e del passo successivo più appropriato.",zh:"让我们讨论您的健康需求以及合适的下一步。",hi:"आइए आपकी स्वास्थ्य आवश्यकताओं और उचित अगले कदम पर चर्चा करें।"},
  "Whether you need a general consultation, patient assessment, primary care, or preventive or follow-up care, I would be happy to discuss your requirements.":{ar:"سواء كنت تحتاج إلى استشارة عامة أو تقييم طبي أو رعاية أولية أو وقائية أو متابعة، يسعدني مناقشة احتياجاتك.",it:"Per una visita generale, una valutazione, cure primarie, prevenzione o follow-up, sarò lieto di discutere le tue esigenze.",zh:"无论您需要一般咨询、患者评估、初级护理、预防护理或随访，我都愿意与您讨论具体需求。",hi:"सामान्य परामर्श, रोगी मूल्यांकन, प्राथमिक, निवारक या फॉलो-अप देखभाल के लिए मैं आपकी आवश्यकताओं पर चर्चा करने के लिए उपलब्ध हूँ।"},
  "Certifications":{ar:"الشهادات والاعتمادات",it:"Certificazioni",zh:"认证",hi:"प्रमाणपत्र"},
  "Education":{ar:"التعليم",it:"Formazione",zh:"教育",hi:"शिक्षा"},
  "Research":{ar:"الأبحاث",it:"Ricerca",zh:"研究",hi:"अनुसंधान"},
  "All Rights Reserved.":{ar:"جميع الحقوق محفوظة.",it:"Tutti i diritti riservati.",zh:"版权所有。",hi:"सर्वाधिकार सुरक्षित।"}
};

const EXTRA_TRANSLATIONS = {};

const FINAL_TRANSLATIONS = {};

const SITE_LANGUAGES = {
    en: {label:"English", flag:"🇬🇧", dir:"ltr"},
    ar: {label:"العربية", flag:"🇸🇦", dir:"rtl"},
    it: {label:"Italiano", flag:"🇮🇹", dir:"ltr"},
    zh: {label:"中文", flag:"🇨🇳", dir:"ltr"},
    hi: {label:"हिन्दी", flag:"🇮🇳", dir:"ltr"}
};

const SITE_TITLES = {
    "index.html": {
        en:"Dr. Asem Al-Zaidani | General Practitioner",
        ar:"د. عاصم الزيداني | طبيب عام",
        it:"Dott. Asem Al-Zaidani | Medico di medicina generale",
        zh:"阿西姆·扎伊达尼医生 | 全科医生",
        hi:"डॉ. आसिम अल-ज़ैदानी | सामान्य चिकित्सक"
    },
    "about.html": {
        en:"About | Dr. Asem Al-Zaidani", ar:"نبذة | د. عاصم الزيداني", it:"Profilo | Dott. Asem Al-Zaidani",
        zh:"简介 | 阿西姆·扎伊达尼医生", hi:"परिचय | डॉ. आसिम अल-ज़ैदानी"
    },
    "resume.html": {
        en:"Resume | Dr. Asem Al-Zaidani", ar:"السيرة الذاتية | د. عاصم الزيداني", it:"Curriculum | Dott. Asem Al-Zaidani",
        zh:"简历 | 阿西姆·扎伊达尼医生", hi:"रिज़्यूमे | डॉ. आसिम अल-ज़ैदानी"
    },
    "portfolio.html": {
        en:"Clinical Activities | Dr. Asem Al-Zaidani", ar:"الأنشطة السريرية | د. عاصم الزيداني", it:"Attività cliniche | Dott. Asem Al-Zaidani",
        zh:"临床活动 | 阿西姆·扎伊达尼医生", hi:"क्लिनिकल गतिविधियाँ | डॉ. आसिम अल-ज़ैदानी"
    },
    "services.html": {
        en:"Services | Dr. Asem Al-Zaidani", ar:"الخدمات | د. عاصم الزيداني", it:"Servizi | Dott. Asem Al-Zaidani",
        zh:"服务 | 阿西姆·扎伊达尼医生", hi:"सेवाएँ | डॉ. आसिम अल-ज़ैदानी"
    },
    "contact.html": {
        en:"Contact | Dr. Asem Al-Zaidani", ar:"التواصل | د. عاصم الزيداني", it:"Contatti | Dott. Asem Al-Zaidani",
        zh:"联系 | 阿西姆·扎伊达尼医生", hi:"संपर्क | डॉ. आसिम अल-ज़ैदानी"
    },
    "assistant.html": {
        en:"Virtual Assistant | Dr. Asem Al-Zaidani", ar:"المساعد الافتراضي | د. عاصم الزيداني", it:"Assistente virtuale | Dott. Asem Al-Zaidani",
        zh:"虚拟助手 | 阿西姆·扎伊达尼医生", hi:"वर्चुअल सहायक | डॉ. आसिम अल-ज़ैदानी"
    }
};

function setupLanguageSystem() {
    const hc = document.querySelector('.header .container');
    if (!hc) return;

    /* Build one stable action area for language + theme + mobile menu. */
    let actions = hc.querySelector('.header-actions');
    if (!actions) {
        actions = document.createElement('div');
        actions.className = 'header-actions';
        const mt = hc.querySelector('.menu-toggle');
        if (mt) {
            hc.insertBefore(actions, mt);
            actions.appendChild(mt);
        } else {
            hc.appendChild(actions);
        }
    }

    let sw = actions.querySelector('.language-switcher');
    if (!sw) {
        sw = document.createElement('div');
        sw.className = 'language-switcher';
        sw.innerHTML = `
            <button type="button" class="language-current" aria-haspopup="listbox" aria-expanded="false" aria-label="English">
                <span class="language-current-flag" aria-hidden="true">🇬🇧</span>
                <span class="sr-only language-current-label">English</span>
            </button>
            <div class="language-menu" role="listbox" aria-label="Language"></div>`;
        actions.insertBefore(sw, actions.querySelector('.menu-toggle'));
    }

    let th = actions.querySelector('.theme-switcher');
    if (!th) {
        th = document.createElement('div');
        th.className = 'theme-switcher';
        th.innerHTML = '<button type="button" class="theme-current" aria-label="Use light theme"><span aria-hidden="true">☼</span></button>';
        actions.insertBefore(th, actions.querySelector('.menu-toggle'));
    }

    const btn = sw.querySelector('.language-current');
    const flag = sw.querySelector('.language-current-flag');
    const label = sw.querySelector('.language-current-label');
    const menu = sw.querySelector('.language-menu');
    if (!btn || !flag || !label || !menu) return;

    /* Rebuild options every time setup runs, but bind only one delegated click handler. */
    menu.innerHTML = Object.entries(SITE_LANGUAGES).map(([code, info]) => `
        <button type="button" class="language-option" data-lang="${code}" role="option" aria-selected="false">
            <span class="language-option-flag" aria-hidden="true">${info.flag}</span>
            <span>${info.label}</span>
        </button>`).join('');

    /*
       Freeze the original English text once.
       This fixes the old "translation of a translation" problem and makes
       language switching deterministic on every page.
    */
    const translatableNodes = [];
    document.querySelectorAll('body *').forEach(el => {
        if (el.closest('.language-switcher,.theme-switcher,script,style,noscript')) return;
        el.childNodes.forEach(node => {
            if (node.nodeType !== Node.TEXT_NODE) return;
            const source = node.textContent.replace(/\s+/g, ' ').trim();
            if (!source || source.length < 2 || /^[+×\/\d\s.\-]+$/.test(source)) return;
            if (!node.__asemI18nSource) node.__asemI18nSource = source;
            translatableNodes.push(node);
        });
    });

    const names = {
        en: 'Dr. Asem Al-Zaidani',
        ar: 'د. عاصم الزيداني',
        it: 'Dott. Asem Al-Zaidani',
        zh: '阿西姆·扎伊达尼医生',
        hi: 'डॉ. आसिम अल-ज़ैदानी'
    };

    const translateText = (source, lang) => {
        if (source.includes('Dr. Asem Al-Zaidani')) {
            return source.replaceAll('Dr. Asem Al-Zaidani', names[lang] || names.en);
        }
        if (lang === 'en') return source;
        return FINAL_TRANSLATIONS[source]?.[lang]
            || EXTRA_TRANSLATIONS[source]?.[lang]
            || SITE_TRANSLATIONS[source]?.[lang]
            || source;
    };

    const applySiteLanguage = (requestedLang) => {
        const lang = SITE_LANGUAGES[requestedLang] ? requestedLang : 'en';
        const info = SITE_LANGUAGES[lang];

        /* Keep the global page geometry LTR. Only the Home hero is mirrored for Arabic. */
        document.documentElement.lang = lang === 'zh' ? 'zh-CN' : lang;
        document.documentElement.dir = 'ltr';
        document.body.dataset.lang = lang;
        document.body.classList.toggle('lang-ar', lang === 'ar');

        flag.textContent = info.flag;
        label.textContent = info.label;
        btn.title = info.label;
        btn.setAttribute('aria-label', info.label);

        menu.querySelectorAll('.language-option').forEach(option => {
            const active = option.dataset.lang === lang;
            option.classList.toggle('active', active);
            option.setAttribute('aria-selected', String(active));
        });

        translatableNodes.forEach(node => {
            const source = node.__asemI18nSource;
            if (source) node.textContent = translateText(source, lang);
        });

        document.querySelectorAll('.logo-name').forEach(el => {
            el.textContent = names[lang] || names.en;
        });

        const file = location.pathname.split('/').pop() || 'index.html';
        if (SITE_TITLES[file]?.[lang]) document.title = SITE_TITLES[file][lang];

        try { localStorage.setItem('asem-site-language', lang); } catch (_) {}
    };

    window.applySiteLanguage = applySiteLanguage;

    /* One reliable click handler. No capture phase and no competing option handlers. */
    if (!sw.dataset.bound) {
        sw.addEventListener('click', event => {
            const option = event.target.closest('.language-option');
            if (option && sw.contains(option)) {
                event.preventDefault();
                event.stopPropagation();
                applySiteLanguage(option.dataset.lang);
                sw.classList.remove('open');
                btn.setAttribute('aria-expanded', 'false');
                return;
            }

            const current = event.target.closest('.language-current');
            if (current && sw.contains(current)) {
                event.preventDefault();
                event.stopPropagation();
                const open = !sw.classList.contains('open');
                sw.classList.toggle('open', open);
                btn.setAttribute('aria-expanded', String(open));
            }
        });

        document.addEventListener('click', event => {
            if (!sw.contains(event.target)) {
                sw.classList.remove('open');
                btn.setAttribute('aria-expanded', 'false');
            }
        });

        btn.addEventListener('keydown', event => {
            if (event.key === 'Escape') {
                sw.classList.remove('open');
                btn.setAttribute('aria-expanded', 'false');
                btn.focus();
            }
            if (event.key === 'Enter' || event.key === ' ') {
                event.preventDefault();
                sw.classList.toggle('open');
                btn.setAttribute('aria-expanded', String(sw.classList.contains('open')));
            }
        });

        sw.dataset.bound = 'true';
    }

    /* First visit is always English. Existing choice is retained only for navigation. */
    let saved = null;
    try { saved = localStorage.getItem('asem-site-language'); } catch (_) {}
    applySiteLanguage(SITE_LANGUAGES[saved] ? saved : 'en');

    const tb = th.querySelector('.theme-current');
    const setTheme = mode => {
        const light = mode === 'light';
        document.body.classList.toggle('theme-light', light);
        if (tb) {
            tb.querySelector('span').textContent = light ? '◐' : '☼';
            tb.title = light ? 'Use dark theme' : 'Use light theme';
            tb.setAttribute('aria-label', tb.title);
        }
        try { localStorage.setItem('asem-site-theme', light ? 'light' : 'dark'); } catch (_) {}
    };

    let theme = 'light';
    try { theme = localStorage.getItem('asem-site-theme') || 'light'; } catch (_) {}
    setTheme(theme);
    if (!tb.dataset.bound) {
        tb.addEventListener('click', () => setTheme(document.body.classList.contains('theme-light') ? 'dark' : 'light'));
        tb.dataset.bound = 'true';
    }
}

/* =====================================
   DR. ASEM PORTFOLIO MAIN JS
===================================== */


// Company logo placeholders: keep clicks inert until real URLs are supplied.
document.addEventListener("click", (event) => {
  const link = event.target.closest('.company-logo-link[href="#"], .client-logo-link[href="#"]');
  if (link) event.preventDefault();
});

document.addEventListener("DOMContentLoaded", () => {

    /* Casual copy/save/drag deterrence. This cannot provide true DRM for web assets. */
    document.addEventListener("contextmenu", event => event.preventDefault());
    document.addEventListener("copy", event => event.preventDefault());
    document.addEventListener("cut", event => event.preventDefault());
    document.addEventListener("dragstart", event => {
        if (event.target.closest?.("img,svg")) event.preventDefault();
    });
    document.addEventListener("keydown", event => {
        const key = event.key.toLowerCase();
        if ((event.ctrlKey || event.metaKey) && ["c","x","s","u"].includes(key)) {
            event.preventDefault();
        }
    });

    try { setupLanguageSystem(); } catch (error) { console.error("Language system initialization failed:", error); }

    /* ==============================
       MOBILE MENU
    ============================== */

    const menuToggle = document.querySelector(".menu-toggle");
    const nav = document.querySelector(".nav");

    if (menuToggle && nav) {
        menuToggle.addEventListener("click", () => {
            nav.classList.toggle("active");
            menuToggle.classList.toggle("open");
        });
    }

    document.querySelectorAll(".nav a").forEach(link => {
        link.addEventListener("click", () => {
            nav?.classList.remove("active");
            menuToggle?.classList.remove("open");
        });
    });


    /* ==============================
       HEADER SCROLL EFFECT
    ============================== */

    const header = document.querySelector(".header");

    const updateHeader = () => {
        header?.classList.toggle("scrolled", window.scrollY > 50);
    };

    updateHeader();
    window.addEventListener("scroll", updateHeader, { passive: true });


    /* ==============================
       SCROLL REVEAL
    ============================== */

    const revealElements = document.querySelectorAll(".reveal");

    if ("IntersectionObserver" in window && revealElements.length) {
        const revealObserver = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add("active");
                    revealObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.3 });

        revealElements.forEach(element => revealObserver.observe(element));
    } else {
        revealElements.forEach(element => element.classList.add("active"));
    }


    /* ==============================
       NUMBER COUNTERS
    ============================== */

    const counters = document.querySelectorAll(".counter");

    const animateCounter = counter => {
        if (counter.dataset.animated === "true") return;
        const target = Number(counter.dataset.target || 0);
        if (!Number.isFinite(target)) return;
        counter.dataset.animated = "true";
        const duration = target <= 20 ? 720 : 920;
        const start = performance.now();
        const ease = progress => progress < .85 ? progress / .85 * .94 : .94 + ((progress - .85) / .3) * .06;
        const tick = now => {
            const progress = Math.min((now - start) / duration, 1);
            counter.textContent = Math.min(target, Math.round(ease(progress) * target));
            if (progress < 1) requestAnimationFrame(tick);
            else counter.textContent = String(target);
        };
        requestAnimationFrame(tick);
    };

    if ("IntersectionObserver" in window && counters.length) {
        const counterObserver = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    animateCounter(entry.target);
                    counterObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.35 });

        counters.forEach(counter => counterObserver.observe(counter));
    } else {
        counters.forEach(counter => {
            counter.textContent = counter.dataset.target || "0";
        });
    }


    /* ==============================
       INTERACTIVE STAT MODALS
    ============================== */

    const modals = document.querySelectorAll(".interactive-modal");
    const closeAllModals = () => {
        modals.forEach(modal => {
            modal.classList.remove("open");
            modal.setAttribute("aria-hidden", "true");
            modal.querySelectorAll("[aria-expanded='true']").forEach(el => el.setAttribute("aria-expanded", "false"));
        });
        document.querySelectorAll(".interactive-stat[aria-expanded='true']").forEach(card => card.setAttribute("aria-expanded", "false"));
        document.body.classList.remove("modal-open");
    };

    document.querySelectorAll(".interactive-stat[aria-controls]").forEach(card => {
        const modal = document.getElementById(card.getAttribute("aria-controls"));
        if (!modal) return;
        const open = () => {
            closeAllModals();
            modal.classList.add("open");
            modal.setAttribute("aria-hidden", "false");
            card.setAttribute("aria-expanded", "true");
            document.body.classList.add("modal-open");
        };
        card.addEventListener("click", open);
        card.addEventListener("keydown", e => {
            if (e.key === "Enter" || e.key === " ") { e.preventDefault(); open(); }
        });
    });

    document.querySelectorAll("[data-close-modal]").forEach(button => button.addEventListener("click", closeAllModals));
    document.addEventListener("keydown", event => {
        if (event.key === "Escape") closeAllModals();
    });

    /* ==============================
       HORIZONTAL PROJECT GALLERY
    ============================== */
    const projectGallery = document.getElementById("projectGallery");
    if (projectGallery) {
        document.querySelector('[data-gallery="prev"]')?.addEventListener("click", () => {
            projectGallery.scrollBy({ left: -projectGallery.clientWidth * .72, behavior: "smooth" });
        });
        document.querySelector('[data-gallery="next"]')?.addEventListener("click", () => {
            projectGallery.scrollBy({ left: projectGallery.clientWidth * .72, behavior: "smooth" });
        });
        let down = false, startX = 0, scrollStart = 0;
        projectGallery.addEventListener("pointerdown", e => {
            down = true; startX = e.clientX; scrollStart = projectGallery.scrollLeft; projectGallery.setPointerCapture(e.pointerId);
        });
        projectGallery.addEventListener("pointermove", e => {
            if (!down) return;
            projectGallery.scrollLeft = scrollStart - (e.clientX - startX);
        });
        projectGallery.addEventListener("pointerup", () => down = false);
        projectGallery.addEventListener("pointercancel", () => down = false);
    }

    /* ============================== CORE EXPERTISE ============================== */
    const expertiseData={
      uc:{title:'Patient Assessment',lead:'A structured, patient-centered approach from history and examination through diagnosis and follow-up.',items:['Clinical history and physical examination','Evidence-based differential diagnosis','Clear treatment and follow-up plans','Patient education and shared decisions']},
      infra:{title:'Primary & Urgent Care',lead:'Practical medical care for common acute conditions and everyday health needs.',items:['Acute illness assessment','Minor injury management','Medication review and counselling','Referral and continuity of care']},
      security:{title:'Chronic Disease Management',lead:'Coordinated monitoring and long-term support for patients with chronic conditions.',items:['Risk-factor assessment','Regular monitoring and review','Lifestyle and preventive counselling','Specialist coordination when required']},
      ai:{title:'Professional Development',lead:'Continuous learning through certified training, clinical workshops and medical research.',items:['BLS and ACLS certification','Continuing medical education','Clinical research participation','Reflective and evidence-based practice']}
    };
    const exTr=t=>{const l=document.documentElement.lang==='zh-CN'?'zh':document.documentElement.lang;return l==='en'?t:(FINAL_TRANSLATIONS[t]?.[l]||EXTRA_TRANSLATIONS[t]?.[l]||SITE_TRANSLATIONS[t]?.[l]||t)};
    const em=document.getElementById('expertiseModal'),et=document.getElementById('expertiseModalTitle'),ek=document.getElementById('expertiseModalKicker'),el=document.getElementById('expertiseModalLead'),ec=document.getElementById('expertiseModalContent');
    const closeEx=()=>{if(!em)return;em.classList.remove('open');em.setAttribute('aria-hidden','true');document.body.classList.remove('expertise-modal-open')};
    const openEx=k=>{const d=expertiseData[k];if(!d||!em)return;ek.textContent=exTr('Core Expertise');et.textContent=exTr(d.title);el.textContent=exTr(d.lead);ec.innerHTML=d.items.map((x,i)=>`<div class="expertise-detail-item" style="--i:${i}"><span>${String(i+1).padStart(2,'0')}</span><p>${exTr(x)}</p></div>`).join('');em.classList.add('open');em.setAttribute('aria-hidden','false');document.body.classList.add('expertise-modal-open')};
    document.querySelectorAll('.expertise-card[data-expertise]').forEach(c=>{const o=()=>openEx(c.dataset.expertise);c.addEventListener('click',o);c.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();o()}})});document.querySelectorAll('[data-close-expertise]').forEach(x=>x.addEventListener('click',closeEx));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeEx()});
    const er=document.querySelectorAll('.expertise-card');
    const expertiseSection=document.querySelector('.expertise');
    if(expertiseSection) document.body.classList.add('expertise-motion-enabled');
    if('IntersectionObserver' in window && er.length){
        const io=new IntersectionObserver(es=>es.forEach(e=>{
            if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}
        }),{threshold:.12,rootMargin:'0px 0px -5% 0px'});
        er.forEach(x=>io.observe(x));
    }else er.forEach(x=>x.classList.add('is-visible'));

    /* ==============================
       CONTACT FORM VALIDATION
    ============================== */

    const contactForm = document.querySelector(".contact-form form");

    if (contactForm) {
        contactForm.addEventListener("submit", event => {
            event.preventDefault();

            let valid = true;

            contactForm.querySelectorAll("input, textarea").forEach(input => {
                if (input.hasAttribute("required") && input.value.trim() === "") {
                    valid = false;
                    input.style.borderColor = "#ff4d4d";
                } else {
                    input.style.borderColor = "";
                }
            });

            if (valid) {
                alert("Thank you for your message. We will contact you soon.");
                contactForm.reset();
            }
        });
    }


    /* ==============================
       EMAIL LINKS
    ============================== */

    document.querySelectorAll('a[href^="mailto:"]').forEach(link => {
        link.addEventListener("click", () => {
            console.log("Email contact initiated");
        });
    });


    /* ==============================
       SMOOTH INTERNAL SCROLL
    ============================== */

    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener("click", event => {
            const selector = anchor.getAttribute("href");

            if (!selector || selector === "#") return;

            const target = document.querySelector(selector);

            if (target) {
                event.preventDefault();
                target.scrollIntoView({ behavior: "smooth", block: "start" });
            }
        });
    });


    /* ==============================
       ACTIVE PAGE
    ============================== */

    const currentPage = window.location.pathname.split("/").pop() || "index.html";

    document.querySelectorAll(".nav a").forEach(link => {
        const linkPage = link.getAttribute("href");

        if (linkPage === currentPage) {
            link.classList.add("active");
        }
    });


    /* ==============================
       EXTERNAL LINKS
    ============================== */

    document.querySelectorAll('a[target="_blank"]').forEach(link => {
        link.setAttribute("rel", "noopener noreferrer");
    });


    /* ==============================
       IMAGE LAZY LOADING
    ============================== */

    document.querySelectorAll("img").forEach(image => {
        if (!image.hasAttribute("loading")) {
            image.setAttribute("loading", "lazy");
        }
    });


    /* ==============================
       LOADER
    ============================== */

    const loader = document.getElementById("loader");

    if (loader) {
        let loaderClosed = false;

        const closeLoader = () => {
            if (loaderClosed) return;
            loaderClosed = true;
            loader.style.opacity = "0";
            loader.style.visibility = "hidden";
            loader.style.pointerEvents = "none";
            setTimeout(() => loader.remove(), 500);
        };

        if (document.readyState === "complete") {
            setTimeout(closeLoader, 180);
        } else {
            window.addEventListener("load", () => setTimeout(closeLoader, 180), { once: true });
        }

        // Never trap the visitor behind the loader if a resource is slow/broken.
        setTimeout(closeLoader, 1800);
    }


    /* ==============================
       LIGHTWEIGHT CUSTOM CURSOR
    ============================== */

    const cursor = document.querySelector(".cursor");
    const cursorOutline = document.querySelector(".cursor-outline");

    if (cursor && cursorOutline && window.matchMedia("(pointer:fine)").matches) {
        window.addEventListener("mousemove", event => {
            cursor.style.transform = `translate3d(${event.clientX - 5}px, ${event.clientY - 5}px, 0)`;
            cursorOutline.style.transform = `translate3d(${event.clientX - 20}px, ${event.clientY - 20}px, 0)`;
        }, { passive: true });
    }

    /* ==============================
       CONSOLE BRANDING
    ============================== */

    console.log(
        "%cDr. Asem Al-Zaidani Portfolio",
        "color:#00b4d8;font-size:18px;font-weight:bold;"
    );

    console.log(
        "General Practitioner | Primary Care | Urgent Care"
    );
});


/* V11 header scroll polish: stays in front and softly fades as scrolling begins. */
(function(){
  const header=document.querySelector('.header');
  if(!header) return;
  let ticking=false;
  const update=()=>{
    const y=window.scrollY||window.pageYOffset||0;
    header.classList.toggle('is-scrolling',y>24);
    ticking=false;
  };
  update();
  window.addEventListener('scroll',()=>{
    if(!ticking){window.requestAnimationFrame(update);ticking=true;}
  },{passive:true});
})();


/* V12 Trusted By touch safeguard: mobile browsers can synthesize :hover after a tap.
   Explicitly keep the marquee running on coarse pointers. */
document.addEventListener('DOMContentLoaded', () => {
  const marquee = document.querySelector('.clients-marquee');
  const track = marquee?.querySelector('.clients-marquee-track');
  if (!marquee || !track) return;
  const coarse = window.matchMedia && window.matchMedia('(pointer: coarse)').matches;
  if (coarse) {
    track.style.animationPlayState = 'running';
    marquee.addEventListener('touchstart', () => { track.style.animationPlayState = 'running'; }, {passive:true});
    marquee.addEventListener('touchend', () => { track.style.animationPlayState = 'running'; }, {passive:true});
  }
});
